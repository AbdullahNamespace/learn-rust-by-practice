// ════════════════════════════════════════════════════════════════════════════
// ## 🔷 Problem 19: circle_area_by_diameter.rs
// ════════════════════════════════════════════════════════════════════════════
// **Category:** Geometry / Mathematics | الهندسة / الرياضيات
// **Difficulty:** Easy | سهل
// **Source:** programmingadvices.com
// ════════════════════════════════════════════════════════════════════════════

// ────────────────────────────────────────────────────────────────────────────
// 📝 DESCRIPTION | الوصف
// ────────────────────────────────────────────────────────────────────────────
//
// EN:
// Write a program that calculates the area of a circle using its diameter (D).
// The program should:
// 1. Read the diameter (D) from the user
// 2. Calculate the area using the formula: Area = (π × D²) / 4
// 3. Display the computed area
//
// This formula is derived from the standard circle area formula (π × R²) by
// substituting R = D/2, which gives: Area = π × (D/2)² = (π × D²) / 4
//
// AR:
// اكتب برنامجًا يحسب مساحة دائرة باستخدام القطر (D).
// يجب على البرنامج:
// 1. قراءة القطر (D) من المستخدم
// 2. حساب المساحة باستخدام الصيغة: المساحة = (π × D²) / 4
// 3. عرض المساحة المحسوبة
//
// هذه الصيغة مشتقة من صيغة مساحة الدائرة القياسية (π × R²) بالتعويض
// R = D/2، مما يعطي: المساحة = π × (D/2)² = (π × D²) / 4
//
// ────────────────────────────────────────────────────────────────────────────

// ────────────────────────────────────────────────────────────────────────────
// 💡 EXAMPLES | الأمثلة
// ────────────────────────────────────────────────────────────────────────────
//
// Example 1:
// Input:  Diameter D = 10.0
// Output: Circle Area ≈ 78.54
//   Why:  Area = (π × 10²) / 4 = (3.14159 × 100) / 4 = 78.53975
//         (Same as radius = 5.0)
//
// Example 2:
// Input:  Diameter D = 20.0
// Output: Circle Area ≈ 314.16
//   Why:  Area = (π × 20²) / 4 = (3.14159 × 400) / 4 = 314.159
//         (Same as radius = 10.0)
//
// Example 3 (Edge Case):
// Input:  Diameter D = 2.0
// Output: Circle Area ≈ 3.14
//   Why:  Area = (π × 2²) / 4 = (3.14159 × 4) / 4 = π ≈ 3.14159
//         (Unit circle with radius = 1.0)
//
// ────────────────────────────────────────────────────────────────────────────

// ────────────────────────────────────────────────────────────────────────────
// ⚠️ CONSTRAINTS | القيود
// ────────────────────────────────────────────────────────────────────────────
//
// • Diameter D must be positive (D > 0) | يجب أن يكون القطر D موجباً
// • Input should be a valid floating-point number | يجب أن يكون الإدخال رقماً عشرياً صحيحاً
// • Formula: Area = (π × D²) / 4 | الصيغة: المساحة = (π × D²) / 4
//
// ────────────────────────────────────────────────────────────────────────────

// ────────────────────────────────────────────────────────────────────────────
// 🔧 FUNCTION SIGNATURES | توقيعات الدوال
// ────────────────────────────────────────────────────────────────────────────
//
// fn read_circle_diameter() -> f64
// fn calculate_circle_area_by_diameter(diameter: f64) -> f64
// fn print_circle_area(area: f64)
//
// ────────────────────────────────────────────────────────────────────────────

use std::io::{self, Write};

// ======================
//      CONSTRAINT
// ======================

const PI: f64 = std::f64::consts::PI;

// ======================
//     INPUT FUNCTIONS
// ======================

fn read_circle_diameter() -> f64 {
    loop {
        let input = read_number::<f64>("Please enter a diameter: ");

        if input > 0.0 {
            return input;
        }

        println!("Error: invalid input, please enter a  positive number!");
    }
}

// ======================
//   PROCESSING FUNCTIONS
// ======================

fn calculate_circle_area_by_diameter(diameter: f64) -> f64 {
    (PI * diameter.powi(2)) / 4.0
}

// ======================
//     OUTPUT FUNCTIONS
// ======================

fn print_circle_area(diameter: f64) {
    println!("╔══════════════════════════════════════════╗");
    println!("║      Circle Area (by Diameter)           ║");
    println!("╠══════════════════════════════════════════╣");
    println!("║  Diameter (D):   {:>15.6}         ║", diameter);
    println!("║  π (Pi):         {:>15.6}         ║", PI);
    println!("╠══════════════════════════════════════════╣");
    println!("║  Area =(π × D²)/4= {:>8.6} × {:.2}² / 4 ║", PI, diameter);
    println!(
        "║  Area =             {:>15.6}      ║",
        calculate_circle_area_by_diameter(diameter)
    );
    println!("╚══════════════════════════════════════════╝");
}

// ======================
//         MAIN
// ======================

fn main() {
    let diameter = read_circle_diameter();

    print_circle_area(diameter);
}

// ======================
//   UTILITY FUNCTIONS
// ======================

fn read_string(prompt: &str) -> Result<String, io::Error> {
    print!("{}", prompt);
    io::stdout().flush()?;

    let mut input = String::new();
    io::stdin().read_line(&mut input)?;

    Ok(input.trim().to_string())
}

fn read_number<T: std::str::FromStr>(prompt: &str) -> T {
    loop {
        let input = match read_string(prompt) {
            Ok(value) => value,
            Err(_) => {
                println!("Error: I/O!");
                continue;
            }
        };

        match input.trim().parse() {
            Ok(value) => return value,
            Err(_) => {
                println!("Error: invalid input, please enter a number!");
            }
        }
    }
}
